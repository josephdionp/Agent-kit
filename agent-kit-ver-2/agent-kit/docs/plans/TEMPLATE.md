# <verb-noun>

<One sentence: what changes for the user or system. Not "add X" — "users can
now Y", "records now propagate to Z".>

## Goal & success criteria

- **Goal:** <the outcome, not the tasks>
- **Done when:**
  - <verifiable check — a test passes, a request returns X, a screen renders Y>
  - <verifiable check>
  - <verifiable check>

## Affected areas

Layer → files. Link design docs; do not restate them.

- Backend: `<path>` — <what changes>
- DB: `<migration file>` — <what changes>
- Frontend: `<path>` — <what changes>
- Design ref: `../project/architecture.md#<section>`

## Steps

Ordered. Each step: what to do + how to verify it landed.

1. **<step name>** — <what>. Verify: `<cmd or check>`.
2. **<step name>** — <what>. Verify: `<cmd or check>`.
3. **<step name>** — <what>. Verify: `<cmd or check>`.

## Risks & rollback

- <risk>: <mitigation or rollback path>

## Out of scope

Explicit non-goals. Prevents scope creep and orphaned "while I was here" changes.

- <thing that could plausibly be included but isn't, and why>
