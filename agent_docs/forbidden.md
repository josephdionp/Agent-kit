# Forbidden

Things that look reasonable but will break this project. Read when in doubt.

Keep this list short and concrete. Every line is a landmine someone stepped on
once. If a line is no longer a landmine (fixed, refactored away), delete it.

## Do not touch without explicit approval

- `<path>` — <why>
- Generated code under `<path>` — regenerated by `<cmd>`

## Do not run without explicit approval

- `<cmd>` — <why: destructive, hits prod, rotates creds, etc.>
- Any migration against a non-local database.

## Patterns that look right but aren't

- <e.g. "Don't add a new logger — use `logger` from `src/log.ts`"> — <why>
- <e.g. "Don't call the ORM directly in handlers — go through the repository
  layer at `src/repo/`"> — <why>

## Dependencies not to add

- <package>: <why — already replaced by X, license issue, security issue>
